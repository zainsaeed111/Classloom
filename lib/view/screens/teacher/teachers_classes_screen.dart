import 'package:flutter/material.dart';

class TeachersClasses extends StatelessWidget {
  const TeachersClasses({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      resizeToAvoidBottomInset: true,

    );
  }
}
