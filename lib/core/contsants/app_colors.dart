import 'package:flutter/widgets.dart';

const Color primaryColor = Color(0xFF093453);
 // Primary call-to-action (CTA), active icons, highlights
   const ctaOrange = Color(0xFFFD9A4D);

  // Informational tags, highlights, badges
   const infoBlue = Color(0xFF92CAF2);

  // Cards, white containers, content background
   const cardBackground = Color(0xFFFAF9F6);

  // Headings, paragraphs
   const textPrimary = Color(0xFF1A2A3B);

  // Semi-transparent orange — highlight selected item, tab bar, dropdowns
   const highlightOverlay = Color(0x33FD9A4D); // 20% opacity

  // Forms, text fields, search bar background
   const inputFieldBackground = Color(0xFFF1F4F8);
  // Success — confirmation messages, positive feedback
   const successGreen = Color(0xFF3BB273);