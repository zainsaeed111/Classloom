import 'package:flutter/widgets.dart';
class AppColors {
   static const Color primaryColor = Color(0xFF093453);

// Primary call-to-action (CTA), active icons, highlights
  static const ctaOrange = Color(0xFFFD9A4D);
   static const secondaryColor = Color(0xFF0B5B8F); // deep sky blue
   static const thirdColor = Color(0xFF128C7E); // teal (like WhatsApp)
   static const Color primaryLightColor = Color(0xFFB3C9D6);
   static const Color primaryExtraLightColor = Color(
       0xFF8FB6C9); // Softer, good for screen backgrounds
   static const Color screenBgColor = Color(0xFFF1F4F8); // Light gray for screens


// Informational tags, highlights, badges
   static const infoBlue = Color(0xFF92CAF2);
   static const textWhite = Color(0xFFFFFFFF);
   static const textBlack = Color(0xFF000000);
  static const  unselectedItemColo= Color(0xFF90A4AE); // Cool Gray


// Cards, white containers, content background
   static const cardBackground = Color(0xFFFAF9F6);

// Headings, paragraphs
   static const textPrimary = Color(0xFF1A2A3B);

// Semi-transparent orange — highlight selected item, tab bar, dropdowns
   static const highlightOverlay = Color(0x33FD9A4D); // 20% opacity

// Forms, text fields, search bar background
   static const inputFieldBackground = Color(0xFFF1F4F8);

// Success — confirmation messages, positive feedback
   static const successGreen = Color(0xFF3BB273);
}
